# =============================================================================
# SUPERVISOR DASHBOARD — copy this file to .env and fill in your values
# =============================================================================

# The Agent Runtime ID returned by: agents-cli deploy
# (leave blank for local development — uses http://localhost:8080)
AGENT_RUNTIME_ID=

# The Cloud Run URL returned by: gcloud run deploy supervisor-dashboard
DASHBOARD_URL=
